net:
  bindIp: {{ bind_host }}
  port: 27017
  unixDomainSocket:
    enabled: true
    pathPrefix: {{ unix_socket_directory }}
    filePermissions: 660

systemLog:
  destination: syslog

storage:
  dbPath: /data/mongodb
